#include <msp430.h> 


/**
 * main.c
 */
int main(void)
{
    WDTCTL = WDTPW | WDTHOLD;   // stop watchdog timer
    PM5CTL0 &= ~LOCKLPM5;
    P4DIR |= 0x01;
    for(;;){
        volatile unsigned int i;
        P4OUT ^= 0x01;
        for(i = 1000000; i > 0; i--)
    }


    return 0;
}
