#include <msp430.h> 


/**
 * main.c
 */
int main(void)
{
	WDTCTL = WDTPW | WDTHOLD;	// stop watchdog timer
	PM5CTL0 &= ~LOCKLPMS;
	P1DIR |= 0x01;
	for(;;){
	    P1OUT ^= 0x01;
	    for(int i = 100000;i > 0; i--)
	    {}
	}

	
	return 0;
}
